@model FCanteen.Web.ViewModels.AssignIngredientsViewModel
@{
    ViewData["Title"] = "GÃ¡n Ä‘á»‹nh lÆ°á»£ng nguyÃªn liá»‡u";
}

<div class="card shadow-sm border-0 mb-4">
    <div class="card-header bg-warning text-dark py-3 d-flex justify-content-between align-items-center">
        <div>
            <h4 class="mb-0 fw-bold"><i class="bi bi-egg-fried me-2"></i>GÃ¡n nguyÃªn liá»‡u cáº¥u thÃ nh mÃ³n Äƒn (YC5)</h4>
            <span class="small">MÃ³n: <strong>@Model.MenuItemName</strong> (@Model.ItemCode) | GiÃ¡ bÃ¡n: <strong>@Model.Price.ToString("N0") Ä‘</strong></span>
        </div>
        <div class="text-end">
            <span class="badge bg-dark fs-6">GiÃ¡ vá»‘n hiá»‡n táº¡i: @Model.CurrentCostPrice.ToString("N0") Ä‘</span>
        </div>
    </div>
    <div class="card-body p-4">
        <form asp-action="AssignIngredients" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="MenuItemId" />
            <input type="hidden" asp-for="MenuItemName" />
            <input type="hidden" asp-for="ItemCode" />
            <input type="hidden" asp-for="Price" />

            <div class="alert alert-info mb-3">
                <i class="bi bi-info-circle me-1"></i>Chá»n cÃ¡c nguyÃªn liá»‡u cáº¥u thÃ nh mÃ³n Äƒn vÃ  Ä‘iá»n Ä‘á»‹nh lÆ°á»£ng (kg/lÃ­t/cÃ¡i). Há»‡ thá»‘ng sáº½ tá»± Ä‘á»™ng tá»•ng há»£p giÃ¡ vá»‘n Ä‘á»ƒ kiá»ƒm tra rÃ ng buá»™c <strong>[MinMargin] (GiÃ¡ bÃ¡n &ge; GiÃ¡ vá»‘n + 20%)</strong>.
            </div>

            <div class="table-responsive mb-4">
                <table class="table table-bordered table-hover align-middle">
                    <thead class="table-light">
                        <tr>
                            <th style="width: 60px;" class="text-center">Chá»n</th>
                            <th>TÃªn nguyÃªn liá»‡u</th>
                            <th style="width: 140px;" class="text-end">GiÃ¡ vá»‘n / ÄÆ¡n vá»‹</th>
                            <th style="width: 200px;">Äá»‹nh lÆ°á»£ng dÃ¹ng cho 1 pháº§n</th>
                            <th style="width: 160px;" class="text-end">ThÃ nh tiá»n giÃ¡ vá»‘n</th>
                        </tr>
                    </thead>
                    <tbody>
                        @for (int i = 0; i < Model.Ingredients.Count; i++)
                        {
                            <tr>
                                <td class="text-center">
                                    <input type="hidden" asp-for="@Model.Ingredients[i].IngredientId" />
                                    <input type="hidden" asp-for="@Model.Ingredients[i].Name" />
                                    <input type="hidden" asp-for="@Model.Ingredients[i].Unit" />
                                    <input type="hidden" asp-for="@Model.Ingredients[i].CostPrice" />
                                    <input class="form-check-input" type="checkbox" asp-for="@Model.Ingredients[i].IsSelected" id="chk_@i" />
                                </td>
                                <td>
                                    <label class="form-check-label fw-bold" for="chk_@i">@Model.Ingredients[i].Name</label>
                                </td>
                                <td class="text-end text-muted">@Model.Ingredients[i].CostPrice.ToString("N0") Ä‘ / @Model.Ingredients[i].Unit</td>
                                <td>
                                    <div class="input-group input-group-sm">
                                        <input asp-for="@Model.Ingredients[i].Quantity" class="form-control text-end fw-semibold" type="number" step="0.01" min="0" />
                                        <span class="input-group-text">@Model.Ingredients[i].Unit</span>
                                    </div>
                                </td>
                                <td class="text-end fw-semibold text-danger">
                                    @((Model.Ingredients[i].Quantity * Model.Ingredients[i].CostPrice).ToString("N0")) Ä‘
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            <div class="d-flex justify-content-between">
                <a asp-action="Index" class="btn btn-outline-secondary"><i class="bi bi-arrow-left me-1"></i>Há»§y bá»</a>
                <button type="submit" class="btn btn-warning fw-bold px-4 shadow-sm"><i class="bi bi-save me-1"></i>LÆ°u cáº¥u hÃ¬nh Ä‘á»‹nh lÆ°á»£ng</button>
            </div>
        </form>
    </div>
</div>